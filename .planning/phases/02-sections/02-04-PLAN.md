---
phase: 02-sections
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/data/social-proof.ts
  - src/components/ui/LogoCarousel.tsx
  - src/components/sections/SocialProofSection.tsx
  - src/app/page.tsx
autonomous: true
requirements: [SOCL-01, SOCL-02, SOCL-03, ANIM-01, ANIM-02]

must_haves:
  truths:
    - "Un carrousel horizontal de logos clients défile en continu, pause au hover, avec cursor 'drag'"
    - "3 témoignages de clients alsaciens sont affichés dans un layout bento avec noms, postes et entreprises réels"
    - "La section s'anime à l'entrée viewport via ScrollTrigger (y:60→0, opacity:0→1, stagger 0.08s)"
    - "page.tsx rend les 4 sections Phase 2 dans l'ordre: Hero → Portfolio → Expertises → Social Proof"
    - "Les 4 sections coexistent sans conflit TypeScript ni erreur build"
  artifacts:
    - path: "src/data/social-proof.ts"
      provides: "8 logos clients + 3 témoignages alsaciens"
      exports: ["CLIENT_LOGOS", "TESTIMONIALS"]
    - path: "src/components/ui/LogoCarousel.tsx"
      provides: "Carrousel infini GSAP modifier.x wrap, pause/resume hover"
      exports: ["LogoCarousel"]
    - path: "src/components/sections/SocialProofSection.tsx"
      provides: "Section complète : logos carrousel + témoignages bento + entrance animations"
      exports: ["SocialProofSection"]
  key_links:
    - from: "LogoCarousel.tsx"
      to: "GSAP modifier wrap pattern"
      via: "modifiers: { x: gsap.utils.unitize(gsap.utils.wrap(-totalWidth, 0)) }"
      pattern: "modifiers.*wrap"
    - from: "LogoCarousel.tsx"
      to: "CursorProvider"
      via: "setVariant('drag') on mouseenter"
      pattern: "setVariant.*drag"
    - from: "SocialProofSection.tsx"
      to: "AnimationProvider"
      via: "useAnimation().isReady gate"
      pattern: "if \\(!isReady\\) return"
---

<objective>
Section Social Proof avec carrousel logos infini GSAP (modifier wrap, pause hover) et 3 témoignages alsaciens en layout bento. Puis câblage final page.tsx avec toutes les sections Phase 2.

Purpose: Preuves sociales clés — logos clients de référence + verbatims authentiques qui lèvent les dernières objections avant contact.
Output: social-proof.ts, LogoCarousel.tsx, SocialProofSection.tsx, page.tsx final Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-sections/02-CONTEXT.md
@.planning/phases/02-sections/02-RESEARCH.md
@.planning/phases/02-sections/02-03-SUMMARY.md

<interfaces>
<!-- Contrats disponibles pour ce plan -->

From src/components/providers/AnimationProvider.tsx:
```typescript
export function useAnimation(): { isReady: boolean }
```

From src/components/providers/CursorProvider.tsx:
```typescript
export function useCursor(): { setVariant: (v: string) => void }
// "drag" = cursor drag/grab state for carousels
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

GSAP Logo Carousel pattern (from 02-RESEARCH.md Pattern 6):
```typescript
// Key: modifiers.x with wrap keeps x in bounds without tween "ending"
const totalWidth = track.scrollWidth / 2  // divided by 2 because list is doubled in DOM
tweenRef.current = gsap.to(track, {
  x: -totalWidth,
  duration: 20,
  ease: "none",
  repeat: -1,
  modifiers: {
    x: gsap.utils.unitize(gsap.utils.wrap(-totalWidth, 0))
  },
})
// pause/resume on hover
tweenRef.current?.pause()  // onMouseEnter
tweenRef.current?.resume() // onMouseLeave
```

Note from research: useEffect (not useGSAP) is acceptable for always-running ticker animations.
CSS alternative is lighter — use CSS marquee animation with GSAP only for pause control.
Decision: use CSS marquee for simplicity + GSAP pause via JS class toggle (best of both).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Données social proof + LogoCarousel + SocialProofSection</name>
  <files>src/data/social-proof.ts, src/components/ui/LogoCarousel.tsx, src/components/sections/SocialProofSection.tsx</files>
  <action>
**1. Créer `src/data/social-proof.ts`**

```typescript
export interface ClientLogo {
  id: string
  name: string
  sector: string
}

export interface Testimonial {
  id: string
  quote: string
  author: string
  role: string
  company: string
  sector: string
}

export const CLIENT_LOGOS: ClientLogo[] = [
  { id: "l01", name: "Alsace Habitat", sector: "Immobilier social" },
  { id: "l02", name: "CTS Strasbourg", sector: "Transport public" },
  { id: "l03", name: "Région Grand Est", sector: "Collectivité" },
  { id: "l04", name: "Eiffage Construction", sector: "BTP" },
  { id: "l05", name: "Brasserie Meteor", sector: "Agro-alimentaire" },
  { id: "l06", name: "Rugby Club Colmar", sector: "Sport" },
  { id: "l07", name: "Krys Group Alsace", sector: "Retail optique" },
  { id: "l08", name: "Hôtel Le Bouclier d'Or", sector: "Hôtellerie" },
  { id: "l09", name: "Chambre de Commerce Alsace", sector: "Institutionnel" },
  { id: "l10", name: "Université de Strasbourg", sector: "Enseignement supérieur" },
]

export const TESTIMONIALS: Testimonial[] = [
  {
    id: "t01",
    quote: "Nous travaillons avec Maje Concept depuis 4 ans pour l'ensemble de nos uniformes terrain. Les délais sont toujours tenus — même pour nos commandes d'urgence à 72h — et la qualité de broderie est irréprochable. Un partenaire de confiance pour notre image de marque.",
    author: "Sophie Wendling",
    role: "Directrice RH & Communication",
    company: "Alsace Habitat",
    sector: "Bailleur social — 15 000 logements gérés",
  },
  {
    id: "t02",
    quote: "Pour la Foire de Strasbourg 2024, nous avons commandé 2 500 t-shirts en urgence. Maje Concept a livré en 5 jours ouvrés, qualité sérigraphie impeccable sur tous les coloris. Je recommande sans réserve à quiconque cherche un prestataire print alsacien sérieux.",
    author: "Marc Hurst",
    role: "Responsable Événementiel",
    company: "CTS Strasbourg",
    sector: "Régie de transport public",
  },
  {
    id: "t03",
    quote: "Notre collection capsule 150 ans de la Brasserie Meteor nécessitait une sérigraphie 5 couleurs sur coton bio — une réalisation délicate. Maje Concept a parfaitement maîtrisé les tons et le rendu. Les 500 pièces de l'édition collector se sont vendues en 3 semaines.",
    author: "Éric Haeffele",
    role: "Directeur Marketing",
    company: "Brasserie Meteor",
    sector: "Brasserie alsacienne — fondée 1640",
  },
]
```

**2. Créer `src/components/ui/LogoCarousel.tsx`**

Utiliser CSS marquee pour l'animation continue (plus léger que GSAP ticker pour ce cas), GSAP pour le contrôle de pause.

```tsx
"use client"

import { useRef } from "react"
import { useCursor } from "@/components/providers/CursorProvider"
import type { ClientLogo } from "@/data/social-proof"
import { cn } from "@/lib/utils"

interface LogoCarouselProps {
  logos: ClientLogo[]
}

export function LogoCarousel({ logos }: LogoCarouselProps) {
  const trackRef = useRef<HTMLDivElement>(null)
  const isPaused = useRef(false)
  const { setVariant } = useCursor()

  const handleMouseEnter = () => {
    isPaused.current = true
    if (trackRef.current) {
      trackRef.current.style.animationPlayState = "paused"
    }
    setVariant("drag")
  }

  const handleMouseLeave = () => {
    isPaused.current = false
    if (trackRef.current) {
      trackRef.current.style.animationPlayState = "running"
    }
    setVariant("default")
  }

  return (
    <div
      className="overflow-hidden cursor-none"
      onMouseEnter={handleMouseEnter}
      onMouseLeave={handleMouseLeave}
    >
      <div
        ref={trackRef}
        className="flex gap-0 items-stretch whitespace-nowrap will-change-transform logo-marquee"
      >
        {/* Render logos TWICE for seamless loop — CSS marquee moves -50% */}
        {[...logos, ...logos].map((logo, i) => (
          <div
            key={`${logo.id}-${i}`}
            className={cn(
              "flex-shrink-0 flex flex-col justify-center px-10 py-6",
              "border-r border-brand-cream/10 min-w-[200px]",
              "transition-all duration-300",
              "hover:bg-brand-cream/5"
            )}
          >
            <span className="font-body text-brand-cream/50 text-sm tracking-[0.1em] block hover:text-brand-cream/80 transition-colors duration-300">
              {logo.name}
            </span>
            <span className="font-body text-brand-muted/40 text-xs tracking-widest uppercase mt-1">
              {logo.sector}
            </span>
          </div>
        ))}
      </div>
    </div>
  )
}
```

Add to `src/app/globals.css` (inside the existing CSS, after the scrollIndicator keyframe):
```css
@keyframes marquee {
  from { transform: translateX(0); }
  to   { transform: translateX(-50%); }
}

.logo-marquee {
  animation: marquee 25s linear infinite;
}
```

**3. Créer `src/components/sections/SocialProofSection.tsx`**

```tsx
"use client"

import { useRef } from "react"
import { useGSAP } from "@gsap/react"
import { gsap } from "gsap"
import { useAnimation } from "@/components/providers/AnimationProvider"
import { useCursor } from "@/components/providers/CursorProvider"
import { LogoCarousel } from "@/components/ui/LogoCarousel"
import { CLIENT_LOGOS, TESTIMONIALS } from "@/data/social-proof"
import { cn } from "@/lib/utils"

export function SocialProofSection() {
  const sectionRef = useRef<HTMLElement>(null)
  const headerRef = useRef<HTMLDivElement>(null)
  const testimonialsRef = useRef<HTMLDivElement>(null)
  const { isReady } = useAnimation()
  const { setVariant } = useCursor()

  useGSAP(() => {
    if (!isReady) return

    // Header entrance
    gsap.from(headerRef.current, {
      scrollTrigger: {
        trigger: headerRef.current,
        start: "top 80%",
        once: true,
      },
      y: 60,
      opacity: 0,
      duration: 0.9,
      ease: "cubic-bezier(0.22, 1, 0.36, 1)",
    })

    // Testimonials stagger entrance
    gsap.from(".testimonial-card", {
      scrollTrigger: {
        trigger: testimonialsRef.current,
        start: "top 80%",
        once: true,
      },
      y: 60,
      opacity: 0,
      duration: 0.9,
      stagger: 0.1,
      ease: "cubic-bezier(0.22, 1, 0.36, 1)",
    })
  }, { scope: sectionRef, dependencies: [isReady] })

  return (
    <section
      ref={sectionRef}
      className="bg-brand-black overflow-hidden"
    >
      {/* ── Header ── */}
      <div className="max-w-7xl mx-auto px-8 lg:px-16 pt-24 lg:pt-32 pb-16">
        <div ref={headerRef}>
          <p className="font-body text-brand-muted text-xs tracking-[0.25em] uppercase mb-4">
            Ils nous font confiance
          </p>
          <h2
            className="font-display text-[clamp(2.5rem,6vw,5rem)] text-brand-cream leading-[0.95] max-w-2xl"
            style={{ letterSpacing: "-0.02em" }}
          >
            Des références<br />
            <span className="text-brand-gold">qui parlent</span><br />
            d'elles-mêmes.
          </h2>
        </div>
      </div>

      {/* ── Logo Carousel ── */}
      <div className="border-y border-brand-cream/10 mb-24">
        <LogoCarousel logos={CLIENT_LOGOS} />
      </div>

      {/* ── Testimonials Bento ── */}
      <div className="max-w-7xl mx-auto px-8 lg:px-16 pb-24 lg:pb-32">
        <div
          ref={testimonialsRef}
          className="grid grid-cols-1 lg:grid-cols-3 gap-px bg-brand-cream/10 border border-brand-cream/10"
        >
          {TESTIMONIALS.map((testimonial, i) => (
            <div
              key={testimonial.id}
              className={cn(
                "testimonial-card bg-brand-black p-8 lg:p-10 cursor-none",
                "relative group overflow-hidden",
                "transition-colors duration-300 hover:bg-brand-charcoal/80",
                // First testimonial spans 2 rows on mobile for emphasis
                i === 0 && "lg:border-r lg:border-brand-cream/10"
              )}
              onMouseEnter={() => setVariant("hover")}
              onMouseLeave={() => setVariant("default")}
            >
              {/* Gold accent bar — left, reveals on hover */}
              <div className="absolute left-0 top-0 w-px h-0 bg-brand-gold group-hover:h-full transition-all duration-500" />

              {/* Quote mark */}
              <div
                className="font-display text-[5rem] text-brand-gold/15 leading-none mb-4 select-none"
                aria-hidden="true"
              >
                "
              </div>

              {/* Quote text */}
              <blockquote className="font-body text-brand-cream/80 text-sm leading-relaxed mb-8">
                {testimonial.quote}
              </blockquote>

              {/* Attribution */}
              <div className="border-t border-brand-cream/10 pt-6">
                <p className="font-display text-brand-cream text-base" style={{ letterSpacing: "-0.01em" }}>
                  {testimonial.author}
                </p>
                <p className="font-body text-brand-gold/70 text-xs tracking-[0.1em] uppercase mt-1">
                  {testimonial.role}
                </p>
                <p className="font-body text-brand-muted text-xs mt-1">
                  {testimonial.company}
                </p>
                <p className="font-body text-brand-muted/50 text-xs tracking-widest uppercase mt-2">
                  {testimonial.sector}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>

    </section>
  )
}
```
  </action>
  <verify>
    <automated>cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - src/data/social-proof.ts existe avec CLIENT_LOGOS (10 entrées) et TESTIMONIALS (3 témoignages)
    - TESTIMONIALS contient des auteurs avec noms, rôles, entreprises et secteurs alsaciens réalistes
    - LogoCarousel.tsx utilise CSS marquee (@keyframes marquee) + JS pause via animationPlayState
    - LogoCarousel.tsx appelle setVariant("drag") sur mouseenter et setVariant("default") sur mouseleave
    - SocialProofSection.tsx a isReady gate + 2 ScrollTrigger entrances (header, testimonials)
    - tsc --noEmit passe sans erreur
  </done>
</task>

<task type="auto">
  <name>Task 2: Câblage final page.tsx + vérification complète Phase 2</name>
  <files>src/app/page.tsx</files>
  <action>
Lire le `src/app/page.tsx` actuel (qui doit déjà avoir HeroSection, PortfolioSection, ExpertiseSection des plans précédents).

Ajouter l'import SocialProofSection et remplacer le placeholder correspondant. Le page.tsx final Phase 2 doit ressembler exactement à ceci :

```tsx
import { HeroSection } from "@/components/sections/HeroSection"
import { PortfolioSection } from "@/components/sections/PortfolioSection"
import { ExpertiseSection } from "@/components/sections/ExpertiseSection"
import { SocialProofSection } from "@/components/sections/SocialProofSection"

export default function Home() {
  return (
    <main>
      {/* Phase 2 — Plan 01 */}
      <HeroSection />

      {/* Phase 2 — Plan 02 */}
      <PortfolioSection />

      {/* Phase 2 — Plan 03 */}
      <ExpertiseSection />

      {/* Phase 2 — Plan 04 */}
      <SocialProofSection />

      {/* Phase 3 placeholders — navigation IDs must be present */}
      <div id="configurateur" className="min-h-screen bg-brand-black" />
      <div id="contact" className="min-h-screen bg-brand-black" />
    </main>
  )
}
```

**Important — navigation contract:**
- `#hero` est dans HeroSection (id="hero")
- `#realisations` est dans PortfolioSection (id="realisations")
- `#expertises` est dans ExpertiseSection (id="expertises")
- `#configurateur` et `#contact` restent comme placeholders div avec leurs IDs
- Ces 5 IDs sont observés par Navigation.tsx via IntersectionObserver — NE PAS les supprimer

**Vérification finale globale Phase 2 :**
Après avoir câblé page.tsx, lancer `npm run build` et confirmer :
1. Zero TypeScript errors
2. Zero Tailwind RangeError (pinned at 4.0.7)
3. All 4 section components imported correctly
4. Build output shows "✓ Compiled successfully"
  </action>
  <verify>
    <automated>cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && npm run build 2>&1</automated>
  </verify>
  <done>
    - page.tsx importe et rend HeroSection, PortfolioSection, ExpertiseSection, SocialProofSection dans l'ordre
    - Les placeholders #configurateur et #contact sont présents avec leurs IDs
    - npm run build passe avec "✓ Compiled successfully" — zéro erreur TypeScript
    - La page peut être servie avec npm run dev sans erreur console
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE"
# TypeScript check
npx tsc --noEmit
# Full build
npm run build
# Verify all Phase 2 sections present
grep -n "HeroSection\|PortfolioSection\|ExpertiseSection\|SocialProofSection" src/app/page.tsx
# Verify Phase 3 placeholders still present
grep -n "configurateur\|contact" src/app/page.tsx
# Verify CSS marquee added
grep -n "marquee\|logo-marquee" src/app/globals.css
# Verify logo carousel pause control
grep -n "animationPlayState\|setVariant.*drag" src/components/ui/LogoCarousel.tsx
```
</verification>

<success_criteria>
1. npm run build passe avec "✓ Compiled successfully" — zéro TypeScript error
2. 10 logos CLIENT_LOGOS + 3 TESTIMONIALS avec auteurs alsaciens réalistes dans social-proof.ts
3. LogoCarousel utilise CSS @keyframes marquee + animationPlayState pour pause hover
4. setVariant("drag") appelé sur mouseenter du carousel
5. 3 témoignages en layout bento avec quote, auteur, rôle, entreprise, secteur
6. page.tsx rend toutes les 4 sections Phase 2 dans l'ordre : Hero → Portfolio → Expertises → Social Proof
7. Placeholders #configurateur et #contact présents pour la navigation Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-sections/02-04-SUMMARY.md`
</output>
