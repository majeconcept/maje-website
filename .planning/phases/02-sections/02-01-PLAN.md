---
phase: 02-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/data/hero.ts
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, ANIM-02]

must_haves:
  truths:
    - "Le visiteur voit le titre héro se révéler ligne par ligne depuis le bas (mask clip) dès la fin du preloader"
    - "Faire bouger la souris dans le hero décale les couches à des vitesses différentes (depth 0.1 / 0.3 / 0.5)"
    - "Faire défiler vers le bas déplace les couches en parallax proportionnellement au scroll (scrub)"
    - "Deux CTAs MagneticButton sont visibles : 'Nos Réalisations' et 'Nous Contacter'"
    - "Toutes les entrées sont orchestrées en stagger 0.08s et déclenchées uniquement quand isReady=true"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Section hero complète avec SplitText, parallax scroll+souris, CTAs"
      exports: ["HeroSection"]
    - path: "src/data/hero.ts"
      provides: "Contenu héro (titre, subtitle, CTAs)"
      exports: ["HERO_CONTENT"]
  key_links:
    - from: "HeroSection.tsx"
      to: "AnimationProvider"
      via: "useAnimation().isReady gate"
      pattern: "if \\(!isReady\\) return"
    - from: "HeroSection.tsx"
      to: "MagneticButton"
      via: "import from @/components/cursor/MagneticButton"
      pattern: "MagneticButton"
    - from: "HeroSection.tsx"
      to: "parallax layers"
      via: "data-depth attribute + GSAP quickTo"
      pattern: "data-depth"
---

<objective>
Section hero immersive avec typographie animée GSAP SplitText (révélation ligne par ligne), parallax multi-couches (scroll + souris), et CTAs MagneticButton.

Purpose: Premier écran visible après le preloader — justifie immédiatement le positionnement ultra-premium via la qualité des animations d'entrée.
Output: HeroSection.tsx composant complet + src/data/hero.ts contenu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sections/02-CONTEXT.md
@.planning/phases/02-sections/02-RESEARCH.md
@.planning/phases/01-fondations/01-03-SUMMARY.md

<interfaces>
<!-- Contrats Phase 1 disponibles pour ce plan -->

From src/components/providers/AnimationProvider.tsx:
```typescript
export function useAnimation(): { isReady: boolean; onPreloaderComplete: () => void }
```

From src/components/providers/CursorProvider.tsx:
```typescript
export function useCursor(): { variant: string; setVariant: (v: string) => void }
```

From src/components/cursor/MagneticButton.tsx:
```typescript
export function MagneticButton({ children, className, ...props }: React.ButtonHTMLAttributes<HTMLButtonElement> & { className?: string }): JSX.Element
// MagneticButton is already built — wraps children in a button with magnetic cursor effect ~80px radius
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/app/globals.css (@theme tokens):
```css
--color-brand-black: oklch(0.10 0.008 250)
--color-brand-gold: oklch(0.72 0.14 82)
--color-brand-cream: oklch(0.96 0.012 88)
--color-brand-charcoal: oklch(0.18 0.008 250)
--color-brand-muted: oklch(0.45 0.010 250)
/* font-display = TestSohneBreit-Dreiviertelfett */
/* font-body = TestSohneBreit-Buch */
```

Section IDs (Navigation contract — DO NOT change):
- `#hero` — this section
- `#realisations` — portfolio (Plan 02)
- `#expertises` — expertises (Plan 03)
- `#configurateur` — Phase 3 placeholder (keep as-is in page.tsx)
- `#contact` — Phase 3 placeholder (keep as-is in page.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Données héro + composant HeroSection</name>
  <files>src/data/hero.ts, src/components/sections/HeroSection.tsx</files>
  <action>
**1. Créer `src/data/hero.ts`**

```typescript
export const HERO_CONTENT = {
  eyebrow: "Marquage textile & print — Alsace",
  titleLines: [
    "L'excellence",
    "du marquage",
    "textile.",
  ],
  subtitle: "Sérigraphie, broderie, DTF, flocage — des réalisations sur-mesure pour les entreprises et institutions qui refusent le compromis.",
  ctaPrimary: { label: "Nos Réalisations", href: "#realisations" },
  ctaSecondary: { label: "Nous Contacter", href: "#contact" },
  badge: "12 ans d'expertise · +2000 clients",
} as const
```

**2. Créer `src/components/sections/HeroSection.tsx`**

```tsx
"use client"

import { useRef } from "react"
import { useGSAP } from "@gsap/react"
import { gsap } from "gsap"
import { SplitText } from "gsap/SplitText"
import { ScrollTrigger } from "gsap/ScrollTrigger"
import { useAnimation } from "@/components/providers/AnimationProvider"
import { useCursor } from "@/components/providers/CursorProvider"
import { MagneticButton } from "@/components/cursor/MagneticButton"
import { cn } from "@/lib/utils"
import { HERO_CONTENT } from "@/data/hero"

export function HeroSection() {
  const sectionRef = useRef<HTMLElement>(null)
  const titleRef = useRef<HTMLHeadingElement>(null)
  const eyebrowRef = useRef<HTMLParagraphElement>(null)
  const subtitleRef = useRef<HTMLParagraphElement>(null)
  const ctasRef = useRef<HTMLDivElement>(null)
  const badgeRef = useRef<HTMLDivElement>(null)
  const { isReady } = useAnimation()
  const { setVariant } = useCursor()

  useGSAP(() => {
    if (!isReady) return
    if (!titleRef.current || !sectionRef.current) return

    // ── ENTRANCE TIMELINE ──────────────────────────────────────────────
    const tl = gsap.timeline()

    // Badge fade in
    tl.from(badgeRef.current, {
      opacity: 0,
      y: 20,
      duration: 0.6,
      ease: "cubic-bezier(0.22, 1, 0.36, 1)",
    })

    // Eyebrow line
    tl.from(eyebrowRef.current, {
      opacity: 0,
      y: 30,
      duration: 0.7,
      ease: "cubic-bezier(0.22, 1, 0.36, 1)",
    }, "-=0.3")

    // SplitText title — new API, mask:"lines" for clean clip reveal
    SplitText.create(titleRef.current, {
      type: "lines",
      mask: "lines",
      autoSplit: true,
      onSplit(self) {
        tl.from(self.lines, {
          yPercent: 100,
          opacity: 0,
          duration: 1.1,
          stagger: 0.1,
          ease: "cubic-bezier(0.22, 1, 0.36, 1)",
          onComplete: () => self.revert(),
        }, "-=0.5")
      },
    })

    // Subtitle + CTAs stagger
    tl.from([subtitleRef.current, ctasRef.current], {
      opacity: 0,
      y: 40,
      duration: 0.9,
      stagger: 0.08,
      ease: "cubic-bezier(0.22, 1, 0.36, 1)",
    }, "-=0.6")

    // ── SCROLL PARALLAX ────────────────────────────────────────────────
    const layers = gsap.utils.toArray<HTMLElement>("[data-depth]", sectionRef.current)

    layers.forEach((layer) => {
      const depth = parseFloat(layer.dataset.depth || "0.2")
      gsap.to(layer, {
        y: () => -(window.innerHeight * depth * 0.5),
        ease: "none",
        scrollTrigger: {
          trigger: sectionRef.current,
          start: "top top",
          end: "bottom top",
          scrub: true,
          invalidateOnRefresh: true,
        },
      })
    })

    // ── MOUSEMOVE PARALLAX ─────────────────────────────────────────────
    const quickToSetters = layers.map((layer) => {
      const depth = parseFloat(layer.dataset.depth || "0.2")
      return {
        x: gsap.quickTo(layer, "x", { duration: 0.8, ease: "power3.out" }),
        y: gsap.quickTo(layer, "y", { duration: 0.8, ease: "power3.out" }),
        depth,
      }
    })

    const onMouseMove = (e: MouseEvent) => {
      const cx = window.innerWidth / 2
      const cy = window.innerHeight / 2
      const dx = (e.clientX - cx) / cx
      const dy = (e.clientY - cy) / cy
      quickToSetters.forEach(({ x, y, depth }) => {
        x(dx * 40 * depth)
        y(dy * 20 * depth)
      })
    }

    const section = sectionRef.current
    section?.addEventListener("mousemove", onMouseMove, { passive: true })

    return () => {
      section?.removeEventListener("mousemove", onMouseMove)
    }
  }, { scope: sectionRef, dependencies: [isReady] })

  return (
    <section
      ref={sectionRef}
      id="hero"
      className="relative min-h-screen overflow-hidden bg-brand-black flex items-center"
    >
      {/* ── PARALLAX LAYER 0.1 — background grain/texture ── */}
      <div
        data-depth="0.1"
        className="absolute inset-0 pointer-events-none"
        aria-hidden="true"
      >
        {/* Noise texture overlay via CSS */}
        <div className="absolute inset-0 opacity-[0.03]"
          style={{
            backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E")`,
            backgroundSize: "256px 256px",
          }}
        />
        {/* Gradient vignette */}
        <div className="absolute inset-0"
          style={{ background: "radial-gradient(ellipse 120% 80% at 50% 60%, transparent 40%, oklch(0.10 0.008 250) 100%)" }}
        />
      </div>

      {/* ── PARALLAX LAYER 0.3 — geometric accent ── */}
      <div
        data-depth="0.3"
        className="absolute inset-0 pointer-events-none"
        aria-hidden="true"
      >
        {/* Diagonal gold line — brand geometric accent */}
        <div className="absolute top-[15%] right-[8%] w-px h-[35vh] bg-brand-gold/20 origin-top"
          style={{ transform: "rotate(15deg)" }}
        />
        <div className="absolute top-[20%] right-[12%] w-px h-[20vh] bg-brand-gold/10 origin-top"
          style={{ transform: "rotate(15deg)" }}
        />
        {/* Bottom left accent */}
        <div className="absolute bottom-[25%] left-[6%] w-24 h-px bg-brand-gold/25" />
        <div className="absolute bottom-[28%] left-[6%] w-12 h-px bg-brand-gold/15" />
      </div>

      {/* ── PARALLAX LAYER 0.5 — main content (foreground) ── */}
      <div
        data-depth="0.5"
        className="relative z-10 w-full max-w-7xl mx-auto px-8 lg:px-16 pt-32 pb-24"
      >
        {/* Badge */}
        <div ref={badgeRef}>
          <span className="inline-block text-brand-gold/70 font-body text-xs tracking-[0.2em] uppercase border border-brand-gold/20 px-4 py-2 mb-8">
            {HERO_CONTENT.badge}
          </span>
        </div>

        {/* Eyebrow */}
        <p
          ref={eyebrowRef}
          className="font-body text-brand-muted text-sm tracking-[0.25em] uppercase mb-6"
        >
          {HERO_CONTENT.eyebrow}
        </p>

        {/* Title — SplitText target */}
        <h1
          ref={titleRef}
          className="font-display text-[clamp(3.5rem,9vw,8rem)] leading-[0.95] text-brand-cream mb-10 max-w-4xl"
          style={{ letterSpacing: "-0.02em" }}
        >
          {HERO_CONTENT.titleLines.join(" ")}
        </h1>

        {/* Subtitle */}
        <p
          ref={subtitleRef}
          className="font-body text-brand-muted text-lg leading-relaxed max-w-xl mb-12"
        >
          {HERO_CONTENT.subtitle}
        </p>

        {/* CTAs */}
        <div ref={ctasRef} className="flex flex-col sm:flex-row gap-4 items-start">
          <MagneticButton
            className={cn(
              "px-8 py-4 bg-brand-gold text-brand-black font-body text-sm tracking-[0.15em] uppercase",
              "hover:bg-brand-cream transition-colors duration-300 cursor-none"
            )}
            onClick={() => {
              document.getElementById("realisations")?.scrollIntoView({ behavior: "smooth" })
            }}
            onMouseEnter={() => setVariant("hover")}
            onMouseLeave={() => setVariant("default")}
          >
            {HERO_CONTENT.ctaPrimary.label}
          </MagneticButton>

          <MagneticButton
            className={cn(
              "px-8 py-4 border border-brand-cream/30 text-brand-cream font-body text-sm tracking-[0.15em] uppercase",
              "hover:border-brand-gold hover:text-brand-gold transition-colors duration-300 cursor-none"
            )}
            onClick={() => {
              document.getElementById("contact")?.scrollIntoView({ behavior: "smooth" })
            }}
            onMouseEnter={() => setVariant("hover")}
            onMouseLeave={() => setVariant("default")}
          >
            {HERO_CONTENT.ctaSecondary.label}
          </MagneticButton>
        </div>

        {/* Scroll indicator */}
        <div className="absolute bottom-12 left-8 lg:left-16 flex items-center gap-3 text-brand-muted/50">
          <div className="w-px h-12 bg-brand-muted/30 relative overflow-hidden">
            <div
              className="absolute top-0 left-0 w-full bg-brand-gold/60"
              style={{ height: "40%", animation: "scrollIndicator 2s ease-in-out infinite" }}
            />
          </div>
          <span className="font-body text-xs tracking-[0.2em] uppercase rotate-0">Défiler</span>
        </div>
      </div>
    </section>
  )
}
```

Add scroll indicator keyframe to `src/app/globals.css` inside the existing `@keyframes` block (or add a new one):
```css
@keyframes scrollIndicator {
  0%   { transform: translateY(-100%); opacity: 0; }
  30%  { opacity: 1; }
  70%  { opacity: 1; }
  100% { transform: translateY(250%); opacity: 0; }
}
```
  </action>
  <verify>
    <automated>cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - src/data/hero.ts existe et exporte HERO_CONTENT
    - src/components/sections/HeroSection.tsx existe, compile sans erreurs TypeScript
    - HeroSection utilise useAnimation().isReady comme première ligne de useGSAP
    - SplitText.create() avec mask:"lines" et onSplit callback présent
    - Trois couches data-depth="0.1", "0.3", "0.5" présentes
    - Deux MagneticButton CTAs présents avec href vers #realisations et #contact
    - tsc --noEmit passe
  </done>
</task>

<task type="auto">
  <name>Task 2: Intégrer HeroSection dans page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
Lire le `src/app/page.tsx` existant (placeholder Phase 1). Remplacer le bloc placeholder `#hero` par l'import et usage de `HeroSection`. Conserver les placeholders `#realisations`, `#expertises`, `#configurateur`, `#contact` intacts — ils seront remplacés dans les plans suivants.

Le page.tsx résultant doit ressembler à ceci (adapter selon le contenu actuel) :

```tsx
import { HeroSection } from "@/components/sections/HeroSection"

export default function Home() {
  return (
    <main>
      {/* Phase 2 Plan 01 */}
      <HeroSection />

      {/* Phase 2 Plan 02 — placeholder remplacé dans 02-02 */}
      <div id="realisations" className="min-h-screen bg-brand-black" />

      {/* Phase 2 Plan 03 — placeholder remplacé dans 02-03 */}
      <div id="expertises" className="min-h-screen bg-brand-black" />

      {/* Phase 3 — placeholders nav tracking */}
      <div id="configurateur" className="min-h-screen bg-brand-black" />
      <div id="contact" className="min-h-screen bg-brand-black" />
    </main>
  )
}
```

**Important:** garder TOUS les section IDs existants. Navigation.tsx les observe via IntersectionObserver — les supprimer casserait le tracking nav active.
  </action>
  <verify>
    <automated>cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - page.tsx importe et rend HeroSection
    - Les IDs #realisations, #expertises, #configurateur, #contact sont présents comme divs placeholder
    - npm run build passe sans erreur TypeScript ni Tailwind RangeError
    - La page s'affiche en dev (npm run dev) avec le hero visible au premier scroll
  </done>
</task>

</tasks>

<verification>
```bash
cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE"
# TypeScript
npx tsc --noEmit
# Build
npm run build
# Vérifier que les fichiers existent
ls src/components/sections/HeroSection.tsx src/data/hero.ts
# Vérifier les imports clés
grep -n "isReady\|SplitText\|data-depth\|MagneticButton" src/components/sections/HeroSection.tsx
```
</verification>

<success_criteria>
1. `npm run build` passe sans erreur — zéro TypeScript error, zéro Tailwind RangeError
2. HeroSection.tsx contient `if (!isReady) return` comme première ligne de useGSAP
3. SplitText.create() avec mask:"lines" et autoSplit:true présent
4. Trois couches parallax data-depth="0.1","0.3","0.5" présentes
5. Deux MagneticButton avec labels "Nos Réalisations" et "Nous Contacter"
6. page.tsx rend HeroSection et conserve tous les IDs de section Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-sections/02-01-SUMMARY.md`
</output>
