---
phase: 01-fondations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - lib/gsapConfig.ts
  - lib/utils.ts
  - public/fonts/TestSohneBreit-Buch.otf
  - public/fonts/TestSohneBreit-Dreiviertelfett.otf
  - public/LOGO VECTO SOURCE.svg
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "`npm run dev` démarre sans erreur sur http://localhost:3000"
    - "La page affiche du texte en police Söhne Breit (pas en fallback system-ui)"
    - "Le fond de la page est noir profond (brand-black), le texte est crème (brand-cream)"
    - "L'inspecteur de styles confirme que les tokens @theme sont actifs (--color-brand-gold, etc.)"
    - "`npm run build` se termine sans erreur (Tailwind 4.0.7 ne déclenche pas le RangeError Turbopack)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "@theme block with palette, typography, easing tokens"
      contains: "--color-brand-black"
    - path: "src/app/layout.tsx"
      provides: "Root layout with next/font/local Söhne Breit + provider shell"
      contains: "localFont"
    - path: "lib/gsapConfig.ts"
      provides: "Centralized GSAP plugin registration (ScrollTrigger, SplitText, Flip)"
      exports: ["registerGSAPPlugins"]
    - path: "lib/utils.ts"
      provides: "cn() class merge utility"
      exports: ["cn"]
    - path: "public/fonts/TestSohneBreit-Buch.otf"
      provides: "Local font file for next/font/local"
    - path: "public/fonts/TestSohneBreit-Dreiviertelfett.otf"
      provides: "Local font file for next/font/local"
  key_links:
    - from: "src/app/layout.tsx"
      to: "public/fonts/TestSohneBreit-Buch.otf"
      via: "next/font/local src array"
      pattern: "localFont.*src.*TestSohneBreit"
    - from: "src/app/globals.css"
      to: "--font-sohne-breit CSS variable"
      via: "@theme { --font-display: var(--font-sohne-breit) }"
      pattern: "var\\(--font-sohne-breit\\)"
    - from: "src/app/layout.tsx"
      to: "lib/gsapConfig.ts"
      via: "registerGSAPPlugins() called at module level"
      pattern: "registerGSAPPlugins"
---

<objective>
Scaffolde le projet Next.js 15, configure Tailwind v4 avec le bloc @theme complet, charge les fonts Söhne Breit via next/font/local, et établit les utilitaires partagés (GSAP config, cn helper).

Purpose: Toute l'infrastructure technique du projet doit exister avant qu'un seul composant d'animation soit écrit. Les tokens de design (@theme), les fonts, et la configuration GSAP sont les fondations que les plans 02 et 03 consomment directement.

Output: Un projet Next.js 15 qui démarre, affiche Söhne Breit, utilise les tokens brand-*, et expose registerGSAPPlugins() + cn() pour les plans suivants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fondations/01-CONTEXT.md
@.planning/phases/01-fondations/01-RESEARCH.md

<interfaces>
<!-- Tokens définis dans ce plan — consommés par plans 02 et 03 -->

Design tokens disponibles après ce plan (globals.css @theme):
```css
--color-brand-black:     oklch(0.10 0.008 250)
--color-brand-charcoal:  oklch(0.18 0.008 250)
--color-brand-gold:      oklch(0.72 0.14 82)
--color-brand-gold-light: oklch(0.82 0.12 82)
--color-brand-cream:     oklch(0.96 0.012 88)
--color-brand-muted:     oklch(0.45 0.010 250)
--font-display: var(--font-sohne-breit), system-ui, sans-serif
--font-body: var(--font-sohne-breit), system-ui, sans-serif
--ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1)
--ease-out-quart: cubic-bezier(0.25, 1, 0.5, 1)
--ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1)
--ease-inout-smooth: cubic-bezier(0.87, 0, 0.13, 1)
--spacing-section: 8rem
```

Exports disponibles après ce plan:
```typescript
// lib/gsapConfig.ts
export function registerGSAPPlugins(): void

// lib/utils.ts
export function cn(...inputs: ClassValue[]): string
```

Classes Tailwind actives (générées par @theme):
bg-brand-black, bg-brand-charcoal, bg-brand-gold, bg-brand-gold-light, bg-brand-cream, bg-brand-muted
text-brand-black, text-brand-charcoal, text-brand-gold, text-brand-cream, text-brand-muted
border-brand-charcoal, border-brand-gold
font-display, font-body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 + pin Tailwind 4.0.7 + install animation deps</name>
  <files>
    package.json,
    public/fonts/TestSohneBreit-Buch.otf,
    public/fonts/TestSohneBreit-Dreiviertelfett.otf,
    public/LOGO VECTO SOURCE.svg
  </files>
  <action>
Le projet n'existe pas encore sur disque. Exécuter dans ce ordre strict:

**Étape 1 — Scaffold:**
```bash
cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE"
npx create-next-app@15 . --typescript --tailwind --app --src-dir --import-alias "@/*" --no-git --yes
```

**Étape 2 — Pin Tailwind 4.0.7 (CRITIQUE — faire immédiatement après scaffold):**
```bash
npm install tailwindcss@4.0.7 @tailwindcss/postcss@4.0.7
```

Ajouter aussi dans package.json une entrée `"overrides"` (npm) pour empêcher une montée de version accidentelle:
```json
"overrides": {
  "tailwindcss": "4.0.7"
}
```

**Étape 3 — Installer les dépendances d'animation:**
```bash
npm install gsap @gsap/react motion lenis
```

**Étape 4 — Installer clsx + tailwind-merge pour cn():**
```bash
npm install clsx tailwind-merge
```

**Étape 5 — Installer les dev tools:**
```bash
npm install -D prettier prettier-plugin-tailwindcss
```

**Étape 6 — Copier les fonts depuis la racine du workspace:**
```bash
mkdir -p "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE/public/fonts"
cp "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/TestSohneBreit-Buch.otf" \
   "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE/public/fonts/TestSohneBreit-Buch.otf"
cp "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/TestSohneBreit-Dreiviertelfett.otf" \
   "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE/public/fonts/TestSohneBreit-Dreiviertelfett.otf"
```

Note: Si les fonts n'existent pas à la racine du workspace, chercher aussi dans:
- `/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE/TestSohneBreit-Buch.otf`
- Le répertoire parent du projet

**Étape 7 — Copier le logo SVG:**
```bash
cp "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/LOGO VECTO SOURCE.svg" \
   "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE/public/LOGO VECTO SOURCE.svg"
```

Note: Si le logo n'est pas trouvé, créer un SVG placeholder à `public/LOGO VECTO SOURCE.svg`:
```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 60" fill="none">
  <text x="0" y="45" font-family="system-ui" font-size="36" font-weight="700" fill="oklch(0.96 0.012 88)">MAJE</text>
</svg>
```

**Étape 8 — Init shadcn (après Tailwind confirmé):**
```bash
npx shadcn@latest init --yes --defaults
```
  </action>
  <verify>
    <automated>
      cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && node -e "const p=require('./package.json'); const tw=p.dependencies?.tailwindcss||p.devDependencies?.tailwindcss; console.log('tailwind:', tw); process.exit(tw?.includes('4.0.7')?0:1)"
    </automated>
  </verify>
  <done>
    - package.json contient tailwindcss à exactement 4.0.7
    - Les dossiers node_modules/gsap, node_modules/lenis, node_modules/motion existent
    - public/fonts/TestSohneBreit-Buch.otf existe (> 0 bytes)
    - public/fonts/TestSohneBreit-Dreiviertelfett.otf existe (> 0 bytes)
    - public/LOGO VECTO SOURCE.svg existe
  </done>
</task>

<task type="auto">
  <name>Task 2: globals.css @theme + layout.tsx avec fonts + lib/gsapConfig.ts + lib/utils.ts + page.tsx placeholder</name>
  <files>
    src/app/globals.css,
    src/app/layout.tsx,
    src/app/page.tsx,
    lib/gsapConfig.ts,
    lib/utils.ts
  </files>
  <action>
**Fichier 1 — src/app/globals.css:**

Remplacer le contenu généré par create-next-app par:

```css
@import "tailwindcss";

@theme {
  /* ─── Brand Palette — OKLCH (perceptually uniform, CSS-native) ─── */
  --color-brand-black:      oklch(0.10 0.008 250);
  --color-brand-charcoal:   oklch(0.18 0.008 250);
  --color-brand-gold:       oklch(0.72 0.14 82);
  --color-brand-gold-light: oklch(0.82 0.12 82);
  --color-brand-cream:      oklch(0.96 0.012 88);
  --color-brand-muted:      oklch(0.45 0.010 250);

  /* ─── Typography ─── */
  /* --font-sohne-breit is injected by next/font/local in layout.tsx */
  --font-display: var(--font-sohne-breit, system-ui, sans-serif);
  --font-body:    var(--font-sohne-breit, system-ui, sans-serif);

  /* ─── Custom Easing — used in GSAP (as strings) and CSS transitions ─── */
  --ease-out-expo:     cubic-bezier(0.16, 1, 0.3, 1);
  --ease-out-quart:    cubic-bezier(0.25, 1, 0.5, 1);
  --ease-spring:       cubic-bezier(0.34, 1.56, 0.64, 1);
  --ease-inout-smooth: cubic-bezier(0.87, 0, 0.13, 1);

  /* ─── Spacing ─── */
  --spacing-section: 8rem;
}

/* ── Base Reset ── */
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  scroll-behavior: auto; /* Lenis handles smooth scroll — disable native */
}

body {
  background-color: var(--color-brand-black);
  color: var(--color-brand-cream);
  font-family: var(--font-body);
  overflow-x: hidden;
}

/* ── Cursor Layers — always above all content, never capture events ── */
.cursor-dot,
.cursor-ring,
.cursor-follower,
.cursor-spotlight {
  pointer-events: none;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 9999;
  will-change: transform;
  /* GSAP writes transform directly — no CSS transition on transform */
}

/* Hide native cursor on fine-pointer (mouse) devices when custom cursor is active */
body.cursor-active * {
  cursor: none !important;
}

/* ── Utility: hide scrollbar visually while Lenis handles scroll ── */
::-webkit-scrollbar {
  display: none;
}
html {
  scrollbar-width: none;
  -ms-overflow-style: none;
}

/* ── Entrance animation keyframe (used by preloader logo fade-in) ── */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(24px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

**Fichier 2 — src/app/layout.tsx:**

```typescript
import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const sohneBreit = localFont({
  src: [
    {
      path: "../../public/fonts/TestSohneBreit-Buch.otf",
      weight: "400",
      style: "normal",
    },
    {
      path: "../../public/fonts/TestSohneBreit-Dreiviertelfett.otf",
      weight: "700",
      style: "normal",
    },
  ],
  variable: "--font-sohne-breit",
  display: "swap",
  preload: true,
});

export const metadata: Metadata = {
  title: "Maje Concept — Impression & Marquage Textile Premium",
  description:
    "Spécialiste du print et marquage textile en Alsace. Sérigraphie, broderie, DTF, flocage — pour entreprises, institutionnels et associations.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" className={sohneBreit.variable}>
      <body className="bg-brand-black text-brand-cream font-display antialiased">
        {/*
          Provider stack injecté par les plans suivants:
          LenisProvider → CursorProvider → AnimationProvider → Navigation + CustomCursor + children
          Ne pas modifier l'ordre.
        */}
        {children}
      </body>
    </html>
  );
}
```

**Fichier 3 — src/app/page.tsx:**

Page placeholder — les sections seront ajoutées en Phase 2. Suffit pour vérifier que le design system est actif.

```typescript
export default function Home() {
  return (
    <main className="min-h-screen">
      {/* Section placeholder — active les tokens @theme pour vérification visuelle */}
      <section
        id="hero-placeholder"
        className="flex min-h-screen flex-col items-center justify-center gap-8 px-8"
      >
        <h1 className="text-5xl font-bold text-brand-gold tracking-tight text-center">
          Maje Concept
        </h1>
        <p className="text-xl text-brand-cream/70 text-center max-w-lg">
          Impression &amp; Marquage Textile Premium — Alsace
        </p>
        <div className="flex gap-4">
          {/* Bouton de test MagneticButton — sera remplacé en Phase 2 */}
          <button className="px-8 py-4 bg-brand-gold text-brand-black font-bold tracking-widest uppercase text-sm hover:bg-brand-gold-light transition-colors duration-200">
            Nos Réalisations
          </button>
          <button className="px-8 py-4 border border-brand-cream/30 text-brand-cream tracking-widest uppercase text-sm hover:border-brand-cream transition-colors duration-200">
            Nous Contacter
          </button>
        </div>

        {/* Token smoke test — masqué visuellement mais confirme que les classes compilent */}
        <div className="hidden">
          <div className="bg-brand-black bg-brand-charcoal bg-brand-gold bg-brand-gold-light bg-brand-cream bg-brand-muted" />
          <div className="text-brand-black text-brand-charcoal text-brand-gold text-brand-cream text-brand-muted" />
          <div className="border-brand-charcoal border-brand-gold" />
        </div>
      </section>
    </main>
  );
}
```

**Fichier 4 — lib/gsapConfig.ts:**

Créer le dossier `lib/` à la racine du projet (pas dans `src/`), ou dans `src/lib/` selon la structure générée par create-next-app. Vérifier d'abord avec `ls src/` — si `src/app/` existe, utiliser `src/lib/`.

```typescript
// src/lib/gsapConfig.ts  (ou lib/gsapConfig.ts selon structure)
"use client";

import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { SplitText } from "gsap/SplitText";
import { Flip } from "gsap/Flip";

/**
 * Centralized GSAP plugin registration.
 * Called once at module level in LenisProvider (plan 02).
 * The `registered` guard prevents double-registration on Next.js hot reloads.
 */
let registered = false;

export function registerGSAPPlugins(): void {
  if (registered) return;
  gsap.registerPlugin(ScrollTrigger, SplitText, Flip);
  registered = true;
}
```

**Fichier 5 — lib/utils.ts:**

```typescript
// src/lib/utils.ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

/**
 * Merge Tailwind classes with conflict resolution.
 * Standard shadcn/ui pattern — used everywhere in the codebase.
 *
 * @example cn("text-brand-cream", condition && "text-brand-gold")
 */
export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

Note sur les chemins: si `create-next-app` a créé `src/app/` (--src-dir flag), alors `lib/` doit être `src/lib/`. Ajuster les imports dans layout.tsx en conséquence (`@/lib/gsapConfig` fonctionne avec l'alias `@/*` → `./src/*`).
  </action>
  <verify>
    <automated>
      cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - `npm run build` se termine avec "✓ Compiled successfully" ou similaire (aucun RangeError, aucun type error)
    - `npm run dev` démarre et http://localhost:3000 affiche du texte en Söhne Breit avec fond noir
    - L'inspecteur de styles (DevTools) confirme: `body` a `font-family: TestSohneBreit...` et `background-color: oklch(0.10 0.008 250)`
    - Les fichiers src/lib/gsapConfig.ts et src/lib/utils.ts existent et exportent les fonctions attendues
  </done>
</task>

</tasks>

<verification>
Vérifications finales après les deux tâches:

```bash
# 1. Le projet démarre
cd "/Users/Max/Library/Mobile Documents/com~apple~CloudDocs/CLAUDE/MAJE-WEBSITE" && npm run dev &
sleep 5 && curl -s http://localhost:3000 | grep -o "Maje Concept" | head -1

# 2. Le build passe (Tailwind 4.0.7 + Turbopack)
npm run build 2>&1 | grep -E "(error|Error|RangeError|✓|Failed)" | head -10

# 3. Les fonts existent
ls -la public/fonts/

# 4. Les tokens @theme sont présents dans globals.css
grep "brand-black\|brand-gold\|brand-cream" src/app/globals.css | wc -l
```
</verification>

<success_criteria>
- `npm run dev` démarre sans erreur
- `npm run build` produit un build propre (aucun RangeError Tailwind)
- http://localhost:3000 affiche "Maje Concept" en Söhne Breit sur fond brand-black
- DevTools confirme que les classes bg-brand-black, text-brand-gold, font-display appliquent les valeurs OKLCH correctes
- public/fonts/ contient les deux fichiers .otf
- src/lib/gsapConfig.ts exporte registerGSAPPlugins()
- src/lib/utils.ts exporte cn()
</success_criteria>

<output>
Après completion, créer `.planning/phases/01-fondations/01-01-SUMMARY.md` avec:
- Stack versions installées (next, tailwindcss, gsap, motion, lenis)
- Chemins réels des fichiers créés (confirmer src/lib/ vs lib/)
- Tout écart par rapport au plan (ex: structure différente de create-next-app)
- Décisions prises (ex: si fonts non trouvées, SVG placeholder utilisé)
</output>
